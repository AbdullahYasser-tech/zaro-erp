# ZARO ERP — حزمة Owner Operations

## المبدأ
تُضاف الميزات الجديدة داخل `zaro_state.data` كأقسام JSON مستقلة حتى لا نكسر البيانات القديمة أو نحتاج إلى نقل سجلات الإنتاج الحالية.

## الأقسام الجديدة

| القسم | الغرض | الصلاحية |
|---|---|---|
| `customers` | ملف العميل وبيانات التواصل والملاحظات | Owner / Accountant |
| `suppliers` | ملف المورد وبيانات التوريد | Owner |
| `returns` | سجل المرتجعات والاسترداد وإعادة التخزين | Owner / Accountant |
| `dailyClosures` | اعتماد إغلاق اليوم مع snapshot للمؤشرات | Owner |
| `expenseApprovals` | اعتماد أو رفض المصروفات | Owner |

## الميزات المشتقة

يحسب التطبيق تنبيهات المخزون المنخفض، الطلبات المتأخرة، فروق التحصيل، المصروفات المعلقة، وعدم إغلاق اليوم. كما يعرض تقريرًا شهريًا مشتقًا من الأوردرات والإعلانات والمصاريف الثابتة، وزر نسخة احتياطية JSON محلية للمالك.

## حدود الأمان

لا تُنشأ بيانات تجريبية في Production. كل عمليات الإدخال الجديدة تمر من `zaro_update_section` مع تحقق خادمي، وتُسجل في `audit_logs`. لا توجد عمليات حذف جماعي، ولا تعديل تلقائي للرصيد عند تسجيل المرتجع؛ إعادة التخزين تتم من خلال حركة مخزون مستقلة بعد المراجعة.
